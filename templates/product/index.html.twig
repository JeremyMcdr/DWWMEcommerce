{% extends 'base.html.twig' %}

{% block title %}{% endblock %}



{% block content %}
    <div class="row">

        <div class="col-md-9">
            <h3>Nos produits</h3>
            <div class="row">
                {% for product in products %}
                    <div class="col-md-3">
                        {% include 'product/single_product.html.twig' %}
                    </div>
                {% endfor %}
            </div>
        </div>

        <div class="col-md-3" id="mon-filtre">
            <h3>Filtrer</h3>
                <div class="mt-4">{{ form(form) }}</div>
        </div>

    </div>
{% endblock %}
